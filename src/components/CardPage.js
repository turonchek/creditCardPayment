import React from 'react';
import { CardImg } from './CardImg';
import { Form } from './Form';

export const CardPage = () => {
    return (
        <div>
            <CardImg/>
            <Form/>
        </div>
    );
}

